<html lang="en">
<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-image: url('https://img.freepik.com/premium-photo/full-size-photo-young-attractive-woman-happy-positive-smile-wear-casual-outfit-isolated-pink-color-background_525549-6199.jpg?w=740');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center top;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* Ensures the form covers the entire viewport height */
        }

        form {
            width: 100%;
            max-width: 800px; /* Increased max-width */
            background-color: #fff;
            padding: 40px; /* Increased padding */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
        }

        select,
        textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            background-color: #c66;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
        }

        button:hover {
            background-color: #c44;
        }
    </style>
</head>
<body>
    <div>
        <h2>Enter Cancellation Reason</h2>
        <form action="/cancel-order/<%= order.id %>" method="POST">
            <label for="predefinedReason">Select a predefined reason:</label>
            <select id="predefinedReason" name="predefinedReason">
                <option value="item_out_of_stock">Size issues</option>
                <option value="changed_mind">Changed my mind</option>
                <option value="Wrong Product Received">Wrong Product Received</option>
                <option value="It's a trial order">It's a trial order</option>
            </select>
        
            <br><br>
        
            <button type="submit">Submit Reason</button>
        </form>
    </div>
</body>
</html>
